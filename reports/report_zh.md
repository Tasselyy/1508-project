# ECE1508 课程项目子实验报告

## 增强RAG的自适应分块：ColBERTv2与双编码器基线检索性能基准测试

---

**课程**: ECE1508 — 高级机器学习系统  
**项目**: 增强RAG的自适应分块（Enhancing RAG with Adaptive Chunking）  
**子实验**: ColBERTv2（后期交互）与双编码器基线（all-MiniLM-L6-v2）基准测试  
**数据集**: KILT NaturalQuestions 开发集  
**报告日期**: 2026年4月

---

## 目录

1. [引言](#1-引言)
2. [假设](#2-假设)
3. [方法论](#3-方法论)
4. [实验设置](#4-实验设置)
5. [结果](#5-结果)
6. [分析](#6-分析)
7. [性能分析](#7-性能分析)
8. [讨论](#8-讨论)
9. [结论](#9-结论)

---

## 1. 引言

### 1.1 项目背景

本报告是ECE1508课程项目"增强RAG的自适应分块"的子实验报告。检索增强生成（Retrieval-Augmented Generation，RAG）是现代大型语言模型（LLM）应用的核心技术之一，其核心思想是在生成回答前先从外部知识库中检索相关文档，从而减少幻觉、提高回答准确性，并支持知识的动态更新。

RAG系统的性能在很大程度上取决于检索阶段的质量。如果检索模块无法找到包含正确答案的文档，后续的生成阶段无论多么强大都无法产生正确结果。因此，选择合适的检索模型以及优化文档的分块（chunking）策略，是构建高性能RAG系统的关键所在。

### 1.2 研究动机

在RAG系统的检索阶段，目前主流的方法可分为两大类：

- **稀疏检索**（Sparse Retrieval）：基于词频统计的方法，如BM25，简单高效但难以捕捉语义相似性。
- **密集检索**（Dense Retrieval）：基于神经网络的语义嵌入方法，能够理解查询的深层语义。密集检索又可进一步分为：
  - **双编码器**（Bi-Encoder）：将查询和文档分别编码为固定维度的向量，通过向量相似度进行检索，速度快但表达能力有限。
  - **交叉编码器**（Cross-Encoder）：将查询和文档拼接后联合编码，精度高但推理速度慢，无法实现大规模索引。
  - **后期交互模型**（Late Interaction）：如ColBERT系列，对查询和文档分别编码出token级别的向量序列，在检索阶段通过MaxSim操作进行细粒度匹配，在精度和效率之间取得良好平衡。

本子实验的核心目的是在受控环境下对比ColBERTv2（后期交互）与双编码器基线（all-MiniLM-L6-v2 + FAISS）的检索性能，特别关注不同查询类型（单实体查询 vs. 多实体查询）下两种方法的表现差异，为后续自适应分块策略的设计提供实证依据。

### 1.3 报告结构

本报告按照标准实验报告格式组织：第2节提出实验假设；第3节详述方法论，包括数据管道、查询分类和评估框架；第4节说明实验设置；第5节汇报量化结果；第6节深入分析实验发现；第7节分析系统性能指标；第8节讨论对更广泛项目的影响；第9节总结关键发现并展望后续工作。

---

## 2. 假设

### 2.1 核心假设

**假设H1（主假设）**：ColBERTv2的token级MaxSim匹配机制在处理多实体查询时表现优于双编码器基线，因为双编码器将所有词汇信息压缩到单一固定维度向量中，导致多个关键实体信息的相互稀释。

### 2.2 假设的理论依据

双编码器方法（如all-MiniLM-L6-v2）通过池化操作（通常为均值池化或CLS token）将整个文本序列的语义压缩为单一向量。这一压缩过程在以下情况下可能导致信息损失：

- 查询包含多个命名实体（如人名、地名、时间、组织名等）时，模型需要在有限的向量维度中同时表征所有实体信息，导致各实体的区分度下降。
- 多个实体之间存在关系约束时，单一向量难以同时编码实体本身及其关系结构。

相比之下，ColBERTv2采用后期交互架构：
- 对文档中每个token独立生成嵌入向量，保留细粒度的token级语义信息。
- 在匹配阶段，通过MaxSim操作为查询的每个token在文档向量集合中寻找最相似的对应token，再对所有查询token的最大相似度求和得到最终得分。
- 这一机制允许多个查询实体分别在文档中找到对应的token级匹配，避免了信息压缩导致的实体混淆。

### 2.3 次级假设

**假设H2**：在单实体查询上，两种方法的性能差距相对较小，因为双编码器能够有效编码单个实体的语义。

**假设H3**：ColBERTv2的性能提升在Recall@1时最为显著，因为MaxSim机制在精确排序方面的优势最为突出。

**假设H4**：ColBERTv2的索引构建时间和存储开销显著高于双编码器，这是其精度优势的代价。

---

## 3. 方法论

### 3.1 数据管道

#### 3.1.1 数据集选择

本实验采用**KILT（Knowledge Intensive Language Tasks）NaturalQuestions（NQ）开发集**作为评估数据集。KILT-NQ是自然问题数据集的标准化版本，专为知识密集型语言任务设计，其特点包括：

- 查询来自真实用户在Google搜索引擎中提出的问题，具有较高的自然性和多样性。
- 每个查询配有对应的gold provenance（黄金来源），即包含正确答案的Wikipedia页面及段落位置信息。
- 提供标准化的Wikipedia知识库接口，便于统一评估。

KILT-NQ开发集共包含约2,800个查询，涵盖历史、科学、地理、文化等多个领域，是评估开放域问答检索性能的标准基准之一。

#### 3.1.2 语料库构建

由于完整的Wikipedia语料库（约600万页面）在有限计算资源下难以完整索引，本实验构建了**缩减版Wikipedia语料库**，策略如下：

- **黄金页面（Gold Pages）**：包含所有开发集查询对应的gold provenance页面，确保正确答案在语料库中可被找到。
- **干扰页面（Distractor Pages）**：从Wikipedia中随机采样额外页面，增加检索难度，模拟真实RAG场景中的噪声环境。
- **最终语料库规模**：约10,000个Wikipedia页面。

这一设计在保证评估有效性（正确答案在语料库中）的同时，将索引规模控制在可管理的范围内，便于在有限硬件条件下完成实验。

#### 3.1.3 文档分块

本实验采用**段落级分块**（Passage-level Chunking）策略：

- 以Wikipedia页面的自然段落为基本分块单元。
- 每个分块（chunk）对应一个语义完整的段落，通常包含50至300个单词。
- 不设置重叠（overlap），分块之间相互独立。
- 分块时保留页面标题信息，作为上下文前缀附加到每个段落前。

段落级分块是RAG系统中最常用的基础策略，为后续自适应分块研究提供了重要的对照基准。

### 3.2 查询分类

为验证假设H1（ColBERTv2在多实体查询上优势更显著），本实验对开发集查询进行了系统化分类。

#### 3.2.1 分类工具

使用**spaCy**（英文模型：`en_core_web_sm`或`en_core_web_trf`）进行命名实体识别（Named Entity Recognition，NER），识别查询中的实体类型包括：

| 实体类型 | 描述 | 示例 |
|----------|------|------|
| PERSON | 人名 | Barack Obama |
| ORG | 组织机构 | NASA, Apple |
| GPE | 国家/城市/州 | France, New York |
| DATE | 日期/时间 | 2024, January |
| LOC | 地点 | Amazon River |
| PRODUCT | 产品 | iPhone |
| EVENT | 事件 | World War II |

#### 3.2.2 分类规则

- **单实体查询**（Single-Entity Queries）：查询中识别到0至1个命名实体的查询。
  - 示例："What is the capital of France?" （1个GPE实体）
  - 示例："Who invented the telephone?" （0个实体，但语义指向单一实体）
  
- **多实体查询**（Multi-Entity Queries）：查询中识别到2个或以上命名实体的查询。
  - 示例："When did Einstein publish the theory of relativity?" （PERSON + DATE + 概念实体）
  - 示例："What movie did Tom Hanks star in that won the Academy Award in 1994?" （PERSON + EVENT + DATE）

#### 3.2.3 样本选取

为保证统计可靠性，从每个类别中各随机采样约500个查询（总计约1,000个评估查询），确保两组样本量均衡，便于直接比较。

### 3.3 检索模型

#### 3.3.1 双编码器基线

**模型**：`sentence-transformers/all-MiniLM-L6-v2`

- **架构**：6层MiniLM Transformer，隐藏层维度384。
- **嵌入维度**：384维密集向量。
- **池化方式**：均值池化（Mean Pooling）。
- **向量索引**：FAISS `IndexFlatIP`（内积精确检索，等价于归一化后的余弦相似度）。
- **检索流程**：
  1. 离线阶段：对所有文档分块编码，生成384维向量，建立FAISS索引。
  2. 在线阶段：对查询编码生成384维向量，通过FAISS进行近邻搜索，返回Top-K结果。

选择all-MiniLM-L6-v2作为基线的理由：
- 模型体量小，推理速度快，适合作为效率基准。
- 在多个开放域问答基准上具有较好的性能，是工业界常用的嵌入模型。
- FAISS `IndexFlatIP`提供精确检索，排除近似索引引入的误差。

#### 3.3.2 ColBERTv2

**框架**：RAGatouille（基于ColBERT官方实现的高级接口）

- **架构**：基于BERT-base的后期交互模型，token嵌入维度128（压缩后）。
- **索引方式**：PLAID（Practical Late Interaction-based Approximate Document retrieval）索引，使用向量量化和倒排索引加速检索。
- **MaxSim评分**：

$$\text{Score}(q, d) = \sum_{i \in q} \max_{j \in d} \mathbf{E}_{q_i} \cdot \mathbf{E}_{d_j}$$

其中 $\mathbf{E}_{q_i}$ 为查询第 $i$ 个token的嵌入向量，$\mathbf{E}_{d_j}$ 为文档第 $j$ 个token的嵌入向量。

- **检索流程**：
  1. 离线阶段：对所有文档分块的每个token编码，建立PLAID压缩索引。
  2. 在线阶段：对查询编码生成token级向量序列，通过PLAID快速候选筛选 + MaxSim精确重排，返回Top-K结果。

### 3.4 评估指标

本实验采用**Recall@k**作为主要评估指标，k取值为1、5、10、20。

$$\text{Recall@k} = \frac{1}{|Q|} \sum_{q \in Q} \mathbb{1}\left[\text{gold passage} \in \text{Top-}k(q)\right]$$

其中 $Q$ 为查询集合，$\text{Top-}k(q)$ 为模型对查询 $q$ 返回的前k个文档分块。若返回的Top-k结果中包含至少一个覆盖gold provenance的分块，则该查询计为成功召回。

Recall@k的优点：
- 直接衡量检索模块能否将正确文档提供给下游生成模块。
- 不同k值反映了不同的使用场景（k=1对应精确检索，k=20对应宽松检索）。
- 易于理解和比较，是开放域问答检索的标准评估指标。

---

## 4. 实验设置

### 4.1 硬件配置

| 硬件组件 | 规格 |
|----------|------|
| 操作系统 | Windows 11 Home |
| CPU | [待基准测试运行后填写] |
| RAM | [待基准测试运行后填写] GB |
| GPU | [待基准测试运行后填写]（VRAM: [待填写] GB）|
| 存储 | [待基准测试运行后填写] |

ColBERTv2的token级编码对GPU内存有较高需求。在离线索引阶段，PLAID索引构建需要将所有文档token向量加载到GPU内存中进行量化处理；在在线检索阶段，查询编码同样需要GPU加速。因此，充足的VRAM是保证ColBERTv2正常运行的关键硬件要求。

### 4.2 语料库规模配置

| 配置项 | 数值 |
|--------|------|
| 总Wikipedia页面数 | ~10,000 |
| 黄金来源页面数 | [待基准测试运行后填写] |
| 干扰页面数 | [待基准测试运行后填写] |
| 总文档分块数（段落级） | [待基准测试运行后填写] |
| 平均每个分块的token数 | [待基准测试运行后填写] |
| 评估查询总数 | ~1,000（每组~500）|

### 4.3 软件版本

| 软件/库 | 版本 |
|---------|------|
| Python | 3.11.x |
| PyTorch | [待基准测试运行后填写] |
| sentence-transformers | [待基准测试运行后填写] |
| faiss-cpu / faiss-gpu | [待基准测试运行后填写] |
| RAGatouille | [待基准测试运行后填写] |
| spaCy | [待基准测试运行后填写] |
| transformers（HuggingFace）| [待基准测试运行后填写] |
| datasets（HuggingFace）| [待基准测试运行后填写] |

### 4.4 实验流程

实验按以下顺序执行：

1. **数据准备**：从KILT下载NQ开发集，构建缩减版Wikipedia语料库。
2. **查询分类**：使用spaCy对所有开发集查询进行NER分类，生成单实体和多实体查询子集。
3. **双编码器索引**：使用all-MiniLM-L6-v2对所有文档分块编码，建立FAISS索引。
4. **ColBERTv2索引**：使用RAGatouille对所有文档分块建立PLAID索引。
5. **检索评估**：对两个查询子集分别运行两种检索模型，计算Recall@1/5/10/20。
6. **性能记录**：记录索引构建时间、在线检索延迟、VRAM使用峰值、索引文件大小等性能指标。
7. **结果汇总与可视化**：生成对比图表，存入 `results/charts/` 目录。

---

## 5. 结果

### 5.1 总体Recall@k对比

下表展示了两种检索模型在所有评估查询（~1,000个）上的总体Recall@k结果：

| 模型 | Recall@1 | Recall@5 | Recall@10 | Recall@20 |
|------|----------|----------|-----------|-----------|
| 双编码器（MiniLM + FAISS）| [待填写] | [待填写] | [待填写] | [待填写] |
| ColBERTv2（RAGatouille）| [待填写] | [待填写] | [待填写] | [待填写] |
| **差值（ColBERT - 双编码器）** | [待填写] | [待填写] | [待填写] | [待填写] |

图5.1展示了两种模型的总体Recall@k对比柱状图：

![Recall@k对比柱状图](../results/charts/recall_bar_chart.png)

*图5.1：ColBERTv2与双编码器基线在不同k值下的Recall对比。*

### 5.2 单实体查询 vs. 多实体查询结果

#### 5.2.1 单实体查询（0-1个实体，~500个查询）

| 模型 | Recall@1 | Recall@5 | Recall@10 | Recall@20 |
|------|----------|----------|-----------|-----------|
| 双编码器（MiniLM + FAISS）| [待填写] | [待填写] | [待填写] | [待填写] |
| ColBERTv2（RAGatouille）| [待填写] | [待填写] | [待填写] | [待填写] |
| **差值（ColBERT - 双编码器）** | [待填写] | [待填写] | [待填写] | [待填写] |

#### 5.2.2 多实体查询（2+个实体，~500个查询）

| 模型 | Recall@1 | Recall@5 | Recall@10 | Recall@20 |
|------|----------|----------|-----------|-----------|
| 双编码器（MiniLM + FAISS）| [待填写] | [待填写] | [待填写] | [待填写] |
| ColBERTv2（RAGatouille）| [待填写] | [待填写] | [待填写] | [待填写] |
| **差值（ColBERT - 双编码器）** | [待填写] | [待填写] | [待填写] | [待填写] |

图5.2展示了Recall@10的分布直方图，用于可视化两种模型在不同查询上的召回分布：

![Recall@10分布直方图](../results/charts/recall_histogram_k10.png)

*图5.2：Recall@10的分布直方图（蓝色：双编码器；橙色：ColBERTv2），分别展示单实体和多实体查询组。*

### 5.3 关键观测

[待基准测试运行后填写具体观测结果。预期观测包括：]

- ColBERTv2在多实体查询上的Recall@k提升幅度是否显著大于单实体查询？
- 两种模型在哪个k值下差距最大？
- 是否存在双编码器表现优于ColBERTv2的查询类型？

---

## 6. 分析

### 6.1 ColBERT vs. 双编码器：核心优劣对比

#### 6.1.1 后期交互的优势

ColBERTv2的MaxSim机制在理论上具有以下优势，这些优势在实验结果中应有所体现：

**细粒度匹配能力**：不同于双编码器将整个文档压缩为单一向量，ColBERTv2为文档的每个token保留独立的嵌入表示。当查询涉及多个具体词汇（如人名、地点、时间等）时，MaxSim能够为每个查询token独立寻找文档中的最佳匹配，避免多个实体信息在向量空间中的相互稀释。

**位置无关的词汇覆盖**：MaxSim操作对文档中实体的位置不敏感，只要文档中包含与查询实体匹配的token，无论其位置如何，都能被MaxSim机制识别并贡献到最终得分。

**查询-文档交互保留**：尽管ColBERTv2的编码是独立进行的（区别于交叉编码器的联合编码），其MaxSim打分仍然保留了部分查询-文档的交互信息，相比纯向量点积具有更强的表达能力。

#### 6.1.2 双编码器的局限性

在本实验场景中，双编码器（all-MiniLM-L6-v2）可能面临以下挑战：

**信息压缩瓶颈**：384维向量需要同时编码查询的所有语义信息。对于"哪部1994年由Tom Hanks主演的电影获得了奥斯卡最佳影片奖？"这类涉及多个约束条件的查询，单一向量可能无法完整表达所有约束，导致检索时无法同时满足所有条件。

**均值池化的稀释效应**：均值池化在处理不均匀重要性词汇时存在固有缺陷——查询中的停用词（如"the"、"of"、"in"）会稀释关键实体词汇的贡献，降低检索精度。

**模型规模限制**：all-MiniLM-L6-v2仅有6层，参数量约22M，相比BERT-base（12层，110M参数）的ColBERTv2在语义理解能力上存在差距。这一差距在困难查询上可能尤为明显。

#### 6.1.3 双编码器的优势

尽管在精度上可能落后，双编码器方法具有以下实际优势：

- **检索速度快**：FAISS向量索引支持毫秒级的精确或近似最近邻搜索，延迟极低。
- **内存占用小**：固定维度向量索引的存储开销远小于ColBERTv2的token级索引。
- **部署简单**：无需GPU即可进行高效推理，降低了部署成本和复杂度。
- **可扩展性强**：双编码器 + FAISS的方案已在工业界得到大规模验证，能够支持数十亿级别的文档索引。

### 6.2 实体组分析

#### 6.2.1 单实体查询分析

对于单实体查询，两种模型的性能差距预计相对较小。单实体查询的特点是语义相对集中，双编码器能够在有限的向量维度中较好地表征核心语义。在这种情况下，ColBERTv2的token级匹配优势相对不那么突出，因为查询中的关键信息主要集中在单一实体词汇上。

[待基准测试运行后填写具体分析结论，包括：实际性能差距是否符合预期？是否存在单实体查询中ColBERTv2明显领先的情况？]

#### 6.2.2 多实体查询分析

多实体查询是验证假设H1的关键实验场景。预期观察：

- ColBERTv2在多实体查询上的Recall@k提升幅度显著大于单实体查询（假设H1为真）。
- 随着查询中实体数量的增加，双编码器的性能下降更为明显，而ColBERTv2相对稳定。
- MaxSim机制能够为不同的查询实体分别找到文档中的匹配token，实现多约束的并行满足。

[待基准测试运行后填写具体分析结论，包括：多实体查询中的性能差距是否显著？是否存在反例（双编码器在某些多实体查询上表现更好）？可能的原因是什么？]

#### 6.2.3 错误案例分析

[待基准测试运行后填写，包括：]

- 双编码器成功但ColBERTv2失败的典型查询案例及失败原因分析。
- ColBERTv2成功但双编码器失败的典型查询案例，验证MaxSim机制的有效性。
- 两种模型均失败的困难查询类型分析，为后续改进提供方向。

---

## 7. 性能分析

### 7.1 时间分解

#### 7.1.1 索引构建时间

| 阶段 | 双编码器（MiniLM + FAISS）| ColBERTv2（RAGatouille）|
|------|---------------------------|--------------------------|
| 文档编码时间 | [待填写] | [待填写] |
| 索引构建时间 | [待填写] | [待填写] |
| 总索引时间 | [待填写] | [待填写] |

#### 7.1.2 在线检索延迟

| 指标 | 双编码器（MiniLM + FAISS）| ColBERTv2（RAGatouille）|
|------|---------------------------|--------------------------|
| 平均查询延迟（ms）| [待填写] | [待填写] |
| P50延迟（ms）| [待填写] | [待填写] |
| P95延迟（ms）| [待填写] | [待填写] |
| P99延迟（ms）| [待填写] | [待填写] |

图7.1展示了两种模型的延迟对比：

![延迟对比图](../results/charts/latency_comparison.png)

*图7.1：ColBERTv2与双编码器基线的查询延迟分布对比（箱线图或小提琴图）。*

### 7.2 VRAM使用情况

| 阶段 | 双编码器（MiniLM + FAISS）| ColBERTv2（RAGatouille）|
|------|---------------------------|--------------------------|
| 索引构建峰值VRAM | [待填写] GB | [待填写] GB |
| 在线检索峰值VRAM | [待填写] GB | [待填写] GB |
| 空闲状态VRAM | [待填写] GB | [待填写] GB |

ColBERTv2由于需要维护token级别的向量矩阵，其VRAM使用预计显著高于双编码器。特别是在索引构建阶段，PLAID的向量量化过程需要将大量token向量加载到GPU内存中。

### 7.3 索引大小对比

| 指标 | 双编码器（MiniLM + FAISS）| ColBERTv2（RAGatouille）|
|------|---------------------------|--------------------------|
| 索引文件大小 | [待填写] MB/GB | [待填写] MB/GB |
| 每个文档分块的平均存储 | [待填写] KB | [待填写] KB |
| 存储空间倍数（ColBERT/双编码器）| [待填写]x | — |

图7.2直观展示了两种模型的索引大小对比：

![索引大小对比图](../results/charts/index_size_comparison.png)

*图7.2：ColBERTv2与双编码器基线的索引文件大小对比（单位：MB）。*

### 7.4 性能-精度权衡分析

基于上述数据，可以从以下维度对两种方法进行综合评估：

- **精度/延迟比**：在相同延迟预算下，哪种模型能达到更高的Recall@k？
- **精度/存储比**：在相同存储预算下，哪种模型能达到更高的Recall@k？
- **规模化潜力**：随着语料库规模从1万页面扩展到100万页面，两种方法的性能如何变化？

[待基准测试运行后填写具体量化分析结论。]

---

## 8. 讨论

### 8.1 对更广泛RAG项目的影响

本子实验的结果将直接影响"增强RAG的自适应分块"项目的技术路线选择：

#### 8.1.1 检索模型选择策略

如果实验结果证实假设H1（ColBERTv2在多实体查询上显著更优），则自适应RAG系统应考虑根据查询类型动态选择检索模型：

- 对于多实体复杂查询，优先使用ColBERTv2进行精细化检索。
- 对于单实体简单查询，使用双编码器快速检索，降低系统延迟。
- 引入查询复杂度分类器（可基于NER实体数量或其他特征），实现检索模型的自动路由。

这一混合检索策略能够在精度和效率之间取得最佳平衡，是自适应分块项目的重要参考方向。

#### 8.1.2 分块策略对检索的影响

本实验采用固定的段落级分块作为基线。实验结果中两种模型的绝对性能（而非相对差异）将揭示当前分块策略的局限性：

- 若两种模型的Recall@1均较低（如低于40%），则说明段落级分块本身存在问题——正确答案可能跨越多个段落，或被切分到无关段落中。
- 若两种模型的Recall@20均较高（如高于80%），则说明分块策略基本合理，主要挑战在于精确排序而非召回。

上述分析将为自适应分块策略的设计提供量化依据，指导后续实验中分块粒度、重叠策略和语义边界识别等方面的优化方向。

#### 8.1.3 计算资源约束下的实践选择

ColBERTv2的精度优势需要以更高的计算成本为代价。在实际的RAG系统部署中，必须综合考虑：

- 延迟要求：若系统要求亚秒级响应，ColBERTv2的重排序开销可能成为瓶颈。
- 硬件成本：ColBERTv2的GPU依赖增加了部署成本，尤其在大规模生产环境中。
- 精度收益：若多实体查询仅占总查询量的一小部分，ColBERTv2的整体精度提升可能有限，不足以证明其额外开销的合理性。

### 8.2 缩减语料库的局限性

本实验采用约10,000个Wikipedia页面的缩减语料库，这一设计引入了以下潜在局限性，在解读结果时需要特别注意：

#### 8.2.1 检索难度的低估

缩减语料库中干扰页面的数量远少于真实的完整Wikipedia（约600万页面）。在较小的候选集中，即使较弱的检索模型也能取得较高的Recall@k。因此，本实验的绝对性能数字可能无法直接外推到真实生产场景，但两种模型之间的**相对差异**具有较好的参考价值。

#### 8.2.2 黄金页面的可用性保证

通过在语料库中强制包含所有gold provenance页面，本实验确保了评估的有效性——正确答案在理论上是可检索的。然而，这一设计也意味着语料库中黄金页面的密度高于真实场景，可能人为提高了两种模型的性能上限。

#### 8.2.3 域外泛化能力

KILT-NQ的查询来自英语用户，且Wikipedia语料库以英语为主。本实验结论在其他语言、领域专属知识库（如医疗、法律、技术文档）或多语言场景下的适用性需要进一步验证。

#### 8.2.4 未来扩展建议

- 在更大规模语料库（如完整KILT Wikipedia或专业领域知识库）上复现实验，验证结论的鲁棒性。
- 引入额外的基准数据集（如TriviaQA、HotpotQA）对比不同任务特性下的检索性能差异。
- 探索稀疏-密集混合检索（Hybrid Retrieval）方案，作为ColBERTv2和双编码器之外的第三条技术路线。

---

## 9. 结论

### 9.1 关键发现总结

本实验系统比较了ColBERTv2（后期交互）与双编码器基线（all-MiniLM-L6-v2 + FAISS）在KILT NaturalQuestions开发集上的检索性能，聚焦于单实体查询和多实体查询两个子集。

[待基准测试运行后，将以下占位符替换为实际发现：]

**发现1（精度对比）**：[待填写 — ColBERTv2是否在总体Recall@k上优于双编码器？优势幅度如何？]

**发现2（实体组对比）**：[待填写 — 假设H1是否得到验证？多实体查询上的性能差距是否显著大于单实体查询？]

**发现3（性能开销）**：[待填写 — ColBERTv2在索引构建时间、在线延迟、VRAM使用和索引大小上的额外开销具体是多少？]

**发现4（实践建议）**：[待填写 — 基于精度-效率权衡，在本实验设置下，哪种检索方法更适合实际RAG部署？]

### 9.2 假设验证

| 假设 | 验证状态 | 备注 |
|------|----------|------|
| H1：ColBERTv2在多实体查询上显著优于双编码器 | [待验证] | [待填写] |
| H2：单实体查询上两种方法差距较小 | [待验证] | [待填写] |
| H3：Recall@1上的优势最为显著 | [待验证] | [待填写] |
| H4：ColBERTv2的资源开销显著更高 | [待验证] | [待填写] |

### 9.3 向自适应分块的后续步骤

本实验作为"增强RAG的自适应分块"项目的基础性子实验，其结果将指导以下后续工作：

1. **自适应分块策略设计**：
   - 基于本实验发现的多实体查询检索挑战，设计能够保留实体关系上下文的语义感知分块策略。
   - 探索基于实体密度的动态分块粒度：实体密集段落采用细粒度分块，实体稀疏段落采用粗粒度分块。

2. **混合检索框架**：
   - 若实验验证ColBERTv2在多实体查询上的优势，设计查询类型路由器，根据查询复杂度自动选择检索策略。
   - 探索双编码器初步召回 + ColBERTv2精确重排的两阶段检索流程，平衡效率与精度。

3. **分块策略的消融实验**：
   - 在固定检索模型的条件下，系统对比段落级分块、句子级分块、滑动窗口分块和语义边界分块的性能差异。
   - 量化分块策略对Recall@k的贡献，与检索模型架构对性能的贡献进行解耦分析。

4. **端到端RAG评估**：
   - 将检索阶段的改进与下游生成阶段结合，以最终问答准确率（Exact Match、F1）评估自适应分块的整体效果。
   - 建立从检索Recall@k到生成准确率的映射关系，为检索优化提供端到端反馈信号。

5. **大规模语料库验证**：
   - 在完整Wikipedia语料库上复现关键实验，验证缩减语料库实验结论的可泛化性。

---

## 参考文献

1. Santhanam, K., Khattab, O., Saad-Falcon, J., Potts, C., & Zaharia, M. (2022). ColBERTv2: Effective and Efficient Retrieval via Lightweight Late Interaction. *NAACL 2022*.

2. Khattab, O., & Zaharia, M. (2020). ColBERT: Efficient and Effective Passage Search via Contextualized Late Interaction over BERT. *SIGIR 2020*.

3. Petroni, F., Piktus, A., Fan, A., Lewis, P., Yazdani, M., De Cao, N., ... & Riedel, S. (2021). KILT: a Benchmark for Knowledge Intensive Language Tasks. *NAACL 2021*.

4. Kwiatkowski, T., Palomaki, J., Redfield, O., Collins, M., Parikh, A., Alberti, C., ... & Toutanova, K. (2019). Natural Questions: A Benchmark for Question Answering Research. *TACL 2019*.

5. Johnson, J., Douze, M., & Jégou, H. (2019). Billion-scale similarity search with GPUs. *IEEE Transactions on Big Data*.

6. Wang, L., Yang, N., Huang, X., Jiao, B., Yang, L., Jiang, D., ... & Wei, F. (2022). Text Embeddings by Weakly-Supervised Contrastive Pre-training. *arXiv:2212.03533*.

7. Reimers, N., & Gurevych, I. (2019). Sentence-BERT: Sentence Embeddings using Siamese BERT-Networks. *EMNLP 2019*.

8. Honnibal, M., Montani, I., Van Landeghem, S., & Boyd, A. (2020). spaCy: Industrial-strength Natural Language Processing in Python.

---

*本报告由ECE1508课程项目团队撰写，所有实验代码及数据处理脚本存储于项目仓库的 `src/` 目录下。图表及原始结果数据存储于 `results/` 目录下。*
